import { Typography } from '@mui/material';
import React from 'react';

export default function NewsPanel() {
    return (
        <div>
            <Typography>bla3o baydate</Typography>
        </div>
    )
}